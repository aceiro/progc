#include <stdio.h>
/* Aqui inicia a funcão principal do código.*/
int main() {
	int target = 10;
	/* Já aqui é definido o valor de target, neste caso ele sera 10. */
	if (target == 10) {
		/* Estamos utilizando um if com a condição de que target é igual a 10, se ele for diferente não ira executar este if.*/
		printf("Target is equal to 10\n");
			/*Já no printf ele ira exibir a mensagem "Target is equal to 10 e pular uma linha."*/
	}
	return 0;
}